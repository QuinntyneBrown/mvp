import { Locator, Page } from '@playwright/test';
import { BasePage } from './base.page';

export class SignInPomPage extends BasePage {
  readonly emailInput: Locator;
  readonly passwordInput: Locator;
  readonly submitButton: Locator;
  readonly errorMessage: Locator;

  constructor(page: Page) {
    super(page);
    this.emailInput = this.byTestId('sign-in-email');
    this.passwordInput = this.byTestId('sign-in-password');
    this.submitButton = this.byTestId('sign-in-submit');
    this.errorMessage = this.byTestId('sign-in-error');
  }

  async goto(): Promise<void> {
    await this.page.goto('/sign-in');
  }

  async signIn(email: string, password: string): Promise<void> {
    await this.emailInput.fill(email);
    await this.passwordInput.fill(password);
    await this.submitButton.click();
  }
}
